# 周末城市探索指南：独立产品 / UX / 可访问性审核

审核日期：2026-09-16  
审核结论：**有条件阻断（无 P0；仍有 5 组 P1 逻辑闭环问题）**

## 1. 审核范围

- 目标：验证“发现活动 → 设置偏好 → AI 规划 / 改线 → 住宿、账单、备忘 → 组队 → 打卡、攻略”的端到端闭环。
- 视口：移动端 390×844、桌面端 1440×1000。
- 设计真值：`/Users/shuweijia/.codex/generated_images/01a0a4f4-e31b-78f2-ae6a-3890c48e6c33/exec-3b1abd44-6b30-4869-8bf2-c21ab9176ddf.png`（853×1844）。
- 实现证据：`qa/01-mobile-itinerary.png` 至 `qa/05-desktop-itinerary.png`、`qa/report.json`，以及上一轮在 Chrome 设备仿真 390×844 上的实际交互走查。
- 自动化结果：`qa/report.json` 中 11 项基础检查通过；未记录 console error、page error 或失败请求；移动端与桌面端均无横向溢出。
- 运行验证：`npm run check:runtime` 通过；`npm run test:sites` 4/4 通过。本审核环境因迁移后目录不在当前写沙箱，无法重新生成 Vite 临时文件；这属于审核环境限制，不是产品构建错误。

## 2. 总体判断

视觉层已经达到可演示水平：地图优先的行程工作台、真实地点图片、蓝 / 白 / 浅灰的 Apple 风格、响应式双栏与移动单栏都成立；活动发现、路线展示、住宿、账单、备忘、组队和旅迹入口也齐全。

当前主要风险不是“页面缺失”，而是若干关键动作只改变吐司或计数，没有改变对应业务数据。面试官连续操作时会发现偏好、AI 改线、AA、组队和内容发布之间不同步，因此在修复 P1 前不建议视为逻辑闭环完成。

## 3. 分步验收

1. **移动端行程首屏：部分通过**  
   地图、路线节点、天气、预算、活动卡和底部导航可见；无横向溢出。参考图在约 390×844 等效高度内展示完整三站和快捷动作，而当前完整页面为 390×1403，浮动底栏还覆盖第二张活动卡的上沿，信息密度与首屏层级有明显漂移（P2）。

2. **探索 → 筛选 → 活动详情：通过，加入行为待补强**  
   筛选、收藏、详情弹层、真实图片和推荐理由均成立。活动卡“加入行程”目前只切换到行程并提示成功；因为三项活动默认已在路线中，用户看不到新增或去重结果（P2）。

3. **偏好 → 推荐 → 行程：未闭环**  
   偏好表单可选择人数、预算、兴趣和雨天规则，但这些值仅存在弹层局部状态。保存后探索页、日期卡、推荐文案和 AA 仍固定为“3 人 / ¥300”，与实际选择不一致（P1）。

4. **AI 生成 / 自然语言改线：未闭环**  
   一键生成能进入既有路线；输入框、快捷提示和 loading / success 吐司可用。但“下雨备选”“再省 ¥50”“加咖啡馆”和任意自定义提示最终执行同一个更新，且把第 3 站写回原时间，站点、费用、天气备选和路线均无可观察变化（P1）。这是产品 AI 价值的核心展示面。

5. **住宿 → 预算 / 账单：未闭环**  
   可选择、切换和移除住宿。行程卡总额采用“账单条目 + 活动价格 + 住宿”，账单弹层总额却只统计账单条目；选择 ¥238 酒店后，行程总额仍按固定 ¥129 计。相邻页面会显示不同总额（P1）。

6. **账单新增 / 删除 / AA：部分通过**  
   新增和删除条目能持久化。AA 永远按 3 人计算，且成员应付结果为固定文案，不会随账单、住宿或偏好人数更新（P1，和偏好状态问题合并修复）。

7. **备忘录：通过**  
   新增、勾选、删除和待办数量联动，且有持久化。建议仅补空状态和误删恢复（P3）。

8. **组队：部分通过**  
   第一张队伍卡可加入 / 退出，并有安全提示。第二张卡点击后只显示“已加入组队”吐司，按钮与数据不变；发起组队提交后也不会把表单中的新队伍插入列表。加入后成员数和剩余名额仍为静态值（P1）。

9. **打卡与攻略：部分通过**  
   表单能提交，统计数字会增加。新打卡 / 新攻略不会进入“最近旅迹”，也不能打开查看刚发布的内容；标题、正文、花费和照片选择未被保存成内容对象（P1）。

10. **桌面端：视觉通过，逻辑风险同移动端**  
    1440×1000 下左侧地图、右侧行程和侧栏导航层级清晰，无横向溢出；核心状态不同步问题在桌面端同样存在。

## 4. 优先级问题

### P0

未发现页面无法加载、核心导航中断、严重布局崩坏或应用自身运行错误。

### P1 — 发布前应修复

1. **偏好没有进入全局行程模型**  
   证据：保存弹层后人数 / 预算 / 兴趣不改变推荐摘要、日期卡、预算判断和 AA。  
   修复：建立持久化 `preferences` 对象；Explore、Itinerary、Bills 和推荐理由统一读取该对象。

2. **AI 改线只返回成功反馈，没有结果差异**  
   证据：所有提示词共用同一 `applyAi` 分支，结果写回原时间。  
   修复：至少为“雨天 / 省预算 / 少走路 / 加咖啡馆”建立确定性的 mock planner；更新 stops、总价、路线说明和“改了几处”，并给出变更前后对比。

3. **住宿、行程总价、账单与 AA 口径不一致**  
   证据：行程卡显示 ¥651，账单弹层只显示条目合计；酒店价格按固定 ¥129；AA 固定除以 3。  
   修复：住宿保存 `{name, price}`；以单一 `tripCost` 派生总额；账单明确区分“实际已支付”和“行程预计”；AA 从偏好人数 / 队伍成员动态计算。

4. **组队状态只覆盖第一张卡，新建队伍不可回看**  
   证据：第二张卡点击无状态变化；新建表单只把全局 `joined` 设为 true。  
   修复：队伍使用数组与独立 id / members / joined 状态；提交后插入列表；加入 / 退出同步名额和按钮。

5. **打卡 / 攻略发布仅增加计数，不保存内容**  
   证据：发布后最近旅迹仍是固定三项。  
   修复：把表单值生成 journal item，插入最近旅迹顶部；支持打开详情，至少保存标题、正文、花费、日期和示例图。

6. **关键发现卡无法用键盘打开**  
   证据：活动卡是带 `onClick` 的 `article`，没有 `role`、`tabIndex` 或键盘处理。  
   修复：使用真实 `button` / `a`，或补齐键盘语义；收藏按钮保持独立可操作。

### P2 — 影响完成度与可信度

1. **移动端密度与参考图明显漂移**：参考图按 390px 宽归一化约 843px 高，当前完整行程为 390×1403；应压缩地图、卡片和间距，或让底栏不覆盖路线卡。  
2. **弹层键盘体验不足**：`role="dialog"` 已有，但未确认初始焦点、焦点圈定、Escape 关闭和关闭后的焦点恢复。  
3. **部分触控目标小于 44px**：收藏 34px、关闭 36px、删除 30px、AI 发送 / 展开 38px。视觉尺寸可保持，但命中区应扩到至少 44×44。  
4. **动作文案强于真实能力**：“加入行程”“已综合天气与预算”“发给同行人”“分享”等部分动作没有相应状态或结果页。不能实现时，应降级为清晰的演示态，而不是成功态。

### P3 — 后续润色

- 为备忘、账单、队伍和旅迹补空状态、撤销与确认。
- 对固定底栏和成功吐司做读屏公告去重；避免短时间连续更新被覆盖。
- 补 200% 放大、长中文文案、Android / iOS 安全区复测。

## 5. 视觉与设计 QA

- **字体与层级**：系统字体 / PingFang 回退正确，标题、正文、标签层级清楚；正文没有明显截断。通过。
- **间距与布局**：桌面双栏比例稳定；移动端整体高出参考图约 66%，底栏与内容产生视觉叠压。P2。
- **颜色与 token**：蓝色主动作、橙色天气警示、冰蓝背景与白色磨砂表面一致，状态色语义清楚。通过。
- **图片与素材**：地图、场馆、市集、滨江图像真实且裁切自然，没有使用 emoji 或 CSS 假图替代。通过。
- **文案**：中文语气自然，推荐理由贴近大学生场景；但若动作未改变数据，成功文案会削弱可信度。P2。
- **桌面响应式**：1440×1000 下无横向溢出，地图 / 行程 / 侧栏边界清楚。通过。

## 6. 可访问性风险

- 已确认优势：主要表单有可见标签或 `aria-label`；焦点样式存在；多数主要按钮达到 44px；图片多有替代文本；成功吐司使用 `role="status"`。
- P1：活动卡不是键盘可达控件。
- P2：弹层缺少可验证的焦点管理和 Escape 关闭；若焦点落到背景，读屏与键盘用户会失去上下文。
- P2：若干图标按钮点击区为 30–38px，移动端误触风险高。
- 验证缺口：尚未完成 VoiceOver / 屏幕阅读器、200% 缩放、减少动态效果、色盲模式的实机验证，不能宣称 WCAG 完全合规。

## 7. 修复顺序与复验门槛

1. 统一 preferences / trip / expenses / team / journal 数据模型。
2. 让至少四类 AI 指令产生可见且不同的行程、费用和说明变化。
3. 修正住宿、账单、总价和 AA 的单一计算口径。
4. 让新建队伍、打卡、攻略进入对应列表并可回看。
5. 修复活动卡键盘语义、弹层焦点和小点击区。
6. 重新采集严格的 **390×844 viewport screenshot**（不是 `fullPage`）与 1440×1000 screenshot；复验上述状态前后差异、刷新持久化、控制台和无横向溢出。

通过门槛：无剩余 P0 / P1；P2 若保留必须有明确取舍说明。

## 8. 证据限制

- `qa/01-mobile-itinerary.png` 是 390×1403 的 full-page 截图，不是设计真值要求的同视口截图；因此它只能证明完整内容和无横向溢出，不能单独证明 390×844 首屏保真。
- 设计真值为 853×1844，等比例归一化到 390px 宽约为 843px 高；本报告据此判断移动端信息密度差异。
- `qa/report.json` 验证的是“控件可点击 / 成功吐司出现”，没有验证业务数据是否实际变化；本报告结合交互走查与当前状态流检查补足这一缺口。

---

## 9. 修复后独立复审（2026-09-16）

### 复审结论

**可交付。** 先前 6 组 P1 已修复并有状态变化证据；本轮未发现新的 P0 / P1。`qa/report.json` 的 14/14 检查通过，console error、page error、失败请求均为 0；`npm run check:runtime` 通过，`npm run test:sites` 4/4 通过。剩余事项均为不阻断本次面试原型交付的 P2 / P3。

### 先前 P1 逐项复验

| 先前问题 | 修复后证据 | 结果 |
| --- | --- | --- |
| 偏好未全局同步 | 人数、预算、兴趣改为持久化状态；自动化把偏好改为 4 人 / ¥400 后，行程日期卡显示“4人 · ¥400/人内”，探索摘要与 AA 同源读取 | 通过 |
| AI 改线仅有吐司 | 雨天、降预算、加咖啡馆、少走路进入不同分支；测试输入“预算再省 50 元”后，路线中出现“龙华会免费创意市集” | 通过 |
| 住宿 / 总账 / AA 口径不一致 | 住宿价格由 `hotelPrices` 统一派生；行程卡和账单弹层使用同一总额；AA 按当前偏好人数计算；测试确认 ¥238 酒店和 4 人 AA 同时可见 | 通过 |
| 第二队申请、新建队伍不落数据 | 第二队可进入“审核中”并持久化；新建“周末同频小队”后，列表出现新卡并显示“已发布” | 通过 |
| 打卡 / 攻略不可回看 | 发布后生成持久化 journal entry；旅迹首屏出现“刚刚发布 · 打卡”和“刚刚发布 · 攻略”两张新卡 | 通过 |
| 活动卡与弹层可访问性不足 | 活动卡加入 `role`、`tabIndex`、Enter / Space；弹层加入初始焦点、Tab 圈定、Escape、焦点恢复；移动端主要控件补齐可访问名称和 44px 命中区 | 通过 |

### 视觉与响应式复验

- `qa/00-mobile-viewport.png` 是严格的 390×844 viewport 截图，补上了上一轮缺失的首屏证据。
- `qa/design-comparison.png` 已将 853×1844 参考图与 390×844 实现放入同一比较画布；地图优先、日期 / 天气上下文、路线节点、AI 白色面板和四项移动导航均保持一致。
- 1440×1000 桌面双栏继续通过，移动端与桌面端均无横向溢出。
- 移动端为了承载住宿、账单、备忘、组队和发布功能，采用比参考图更长的可滚动内容；首屏底栏与下一张路线卡有视觉叠放，但第一张卡和主任务入口不受阻，作为本次高保真交互原型可接受。

### 剩余非阻断项

1. **P2 — 预计预算与已记录总账可进一步区分**：AI 把活动改为免费时，路线卡发生变化，但总账仍代表已记录消费。建议后续明确标成“预计预算”和“已记录账单”两个口径，避免用户把 AI 节省金额与实际账单混为一谈。
2. **P2 — 组队可继续细化为逐队成员模型**：当前第一队仍使用全局 `joined`，成员数和剩余名额为静态展示；发布自己的队伍不应影响其他队伍的加入状态。后续可统一为按 team id 保存 `status / members / capacity`。
3. **P2 — 打卡记录可补花费字段回显**：新打卡已可回看标题和说明，但表单中的“本次花费”尚未进入 journal entry 卡片。
4. **P3 — 语义结构可继续收紧**：活动卡已能用键盘打开，但 `role="button"` 的 article 内仍包含收藏按钮。后续可改为独立详情链接与收藏按钮，减少嵌套交互语义。
5. **验证边界**：自动化已覆盖键盘所需结构和点击区，但尚未执行 VoiceOver、200% 缩放与减少动态效果实机复测，因此不宣称完整 WCAG 合规。

### 最终交付门槛

- 产品 / 功能闭环：通过。
- 移动端优先与桌面响应式：通过。
- 设计真值对比：通过，保留已说明的内容密度适配。
- 基础可访问性与运行稳定性：通过。
- 是否可部署并提交面试链接：**是。**
